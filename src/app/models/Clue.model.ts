export  interface Mystery {
  title: string;
  announcements: string[];
}

export interface MysteryStats {
  solvedMysteryCount: number;
  suspectCount: number;
  sceneCount: number;
  weaponCount: number;
}
